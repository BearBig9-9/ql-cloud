spring:
  application:
    name: api-gateway
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
      profile: dev
      label: release-v1.0
eureka:
  client:
    service-url:
      defaultZone: http://localhost:1111/eureka/
server:
  port: 9090